#!/usr/bin/env bash
set -euo pipefail

case "${1:-}" in
    hidden) script=hidden-apps.sh ;;
    system) script=system-apps.sh ;;
    *) printf 'Usage: %s {hidden|system}\n' "$0" >&2; exit 2 ;;
esac
shift
exec "${XDG_CONFIG_HOME:-$HOME/.config}/rofi/scripts/$script" "$@"
