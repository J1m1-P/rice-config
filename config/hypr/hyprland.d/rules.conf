# Window rules
windowrule = match:class ^(blueman-manager)$, float on
windowrule = match:class ^(nm-connection-editor)$, float on

# Subtle blur for Waybar's translucent layer surface.
layerrule = blur on, match:namespace ^waybar$
